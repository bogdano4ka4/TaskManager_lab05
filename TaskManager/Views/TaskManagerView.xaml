<UserControl x:Class="TaskManager.Views.TaskManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <StatusBar Grid.Row="0" Height="30" HorizontalAlignment="Stretch">
            <Label HorizontalAlignment="Center"  VerticalAlignment="Center"  Content="Sort: "></Label>
           <ComboBox SelectedIndex="{Binding SortIndex,  UpdateSourceTrigger=PropertyChanged}"  Height="30" VerticalAlignment="Top">
                <TextBlock>Name</TextBlock>
                <TextBlock>Id</TextBlock>
                <TextBlock>CPU</TextBlock>
                <TextBlock>Operating system</TextBlock>
                <TextBlock>Memory</TextBlock>
                <TextBlock>Threads count</TextBlock>
                <TextBlock>User Name</TextBlock>
            </ComboBox>
        </StatusBar>
        <DataGrid Grid.Row="1" Grid.Column="0" HorizontalAlignment="Center"  ItemsSource="{Binding Processes, Mode=OneWay}" SelectedItem ="{Binding SelectedProcess}" AutoGenerateColumns="False"  >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name"  Binding="{Binding Name}"></DataGridTextColumn>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}"></DataGridTextColumn>
                <DataGridTextColumn Header="CPU, %" Binding="{Binding CPU}"></DataGridTextColumn>
                <DataGridTextColumn Header="Operating Memory"  IsReadOnly="True" Binding="{Binding OperatingMemory}"></DataGridTextColumn>
                <DataGridTextColumn Header="Threads Count"  IsReadOnly="True" Binding="{Binding ThreadsCount}"></DataGridTextColumn>
                <DataGridTextColumn Header="User Name"  IsReadOnly="True" Binding="{Binding UserName}"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        <StatusBar Grid.Row="2" Height="30" HorizontalAlignment="Stretch"  Grid.Column="0"
                   VerticalAlignment="Bottom">
            <Button  Command="{Binding ShowModulesCommand}">
                <TextBlock Text="Show Modules" FontWeight="Bold"></TextBlock>
            </Button>
            <Button HorizontalAlignment="Center" Command="{Binding ShowThreadsCommand}">
                <TextBlock Text="Show Threads" FontWeight="Bold"></TextBlock>
            </Button>

            <Button HorizontalAlignment="Center" Command="{Binding StopProcessCommand}">
                <TextBlock Text="Stop process" FontWeight="Bold"></TextBlock>
            </Button>
            <Button HorizontalAlignment="Center" Command="{Binding OpenFolderCommand}">
                <TextBlock Text="Open Folder " FontWeight="Bold"></TextBlock>
            </Button>
        </StatusBar>

    </Grid>
</UserControl>
